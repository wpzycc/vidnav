# Cloudflare Pages Configuration
# This file configures Cloudflare Pages deployment settings

# Project name
name = "vidnav"

# Compatibility settings for Cloudflare Workers runtime
compatibility_date = "2024-11-11"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"
# Pages-specific configuration
# Note: For Next.js on Cloudflare Pages, the build output is in .next/
# NOT .next/standalone (standalone is for self-hosted deployments)

# Build configuration is handled by Cloudflare Pages dashboard or CLI
# Environment variables should be set in the dashboard Settings > Environment variables
[env.production.vars]
GITHUB_CLIENT_ID = "Ov23liK5fCEuJefVswb5"
GITHUB_OWNER = "wpzycc"
GITHUB_REPO = "vidnav"
GITHUB_BRANCH = "main"
NEXTAUTH_URL = "https://vidnav-11i.pages.dev/api/auth"
NEXT_PUBLIC_API_URL="https://vidnav-11i.pages.dev"
